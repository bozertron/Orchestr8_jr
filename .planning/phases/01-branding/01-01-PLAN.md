---
phase: 01-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - IP/plugins/06_maestro.py
  - IP/woven_maps.py
  - IP/woven_maps_nb.py
autonomous: true

must_haves:
  truths:
    - "User sees 'orchestr8' in top row brand display (not 'stereOS')"
    - "Browser DevTools shows CSS classes prefixed with '.orchestr8-*' (not '.stereos-*')"
    - "Developer reading docstrings sees 'orchestr8' references (not 'stereOS')"
  artifacts:
    - path: "IP/plugins/06_maestro.py"
      provides: "Brand display and CSS classes"
      contains: "orchestr8-brand"
    - path: "IP/woven_maps.py"
      provides: "Code City visualization"
      contains: "orchestr8"
    - path: "IP/woven_maps_nb.py"
      provides: "Code City notebook"
      contains: "orchestr8"
  key_links:
    - from: "IP/plugins/06_maestro.py"
      to: "browser rendering"
      via: "HTML template string"
      pattern: "orchestr8-brand.*orchestr8-prefix"
---

<objective>
Replace all "stereOS" branding references with "orchestr8" across the active codebase.

Purpose: Establish consistent "orchestr8" branding throughout the application so users see the correct product name in the UI and developers see consistent naming in documentation.

Output: All visible text, CSS classes, and docstrings will reference "orchestr8" instead of "stereOS".
</objective>

<execution_context>
@/home/bozertron/.claude/get-shit-done/workflows/execute-plan.md
@/home/bozertron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@SOT/WIRING_PLAN.md

Key files to modify:
- IP/plugins/06_maestro.py (primary - brand text, CSS classes, docstrings)
- IP/woven_maps.py (docstrings only)
- IP/woven_maps_nb.py (docstrings only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace brand text and CSS classes in 06_maestro.py</name>
  <files>IP/plugins/06_maestro.py</files>
  <action>
    Replace the following in IP/plugins/06_maestro.py:

    1. **Brand display text** (lines 873-876):
       - Change `<div class="stereos-brand">` to `<div class="orchestr8-brand">`
       - Change `<span class="stereos-prefix">stere</span><span class="stereos-suffix">OS</span>` to `<span class="orchestr8-prefix">orchestr</span><span class="orchestr8-suffix">8</span>`

    2. **CSS class definitions** (lines 189-200):
       - Change `.stereos-brand` to `.orchestr8-brand`
       - Change `.stereos-prefix` to `.orchestr8-prefix`
       - Change `.stereos-suffix` to `.orchestr8-suffix`

    3. **Docstrings** (lines 5, 7, 11):
       - Line 5: Change "inspired by stereOS MaestroView" to "inspired by MaestroView"
       - Line 7: Change "(stereOS | Collabor8 | JFDI | Gener8)" to "(orchestr8 | Collabor8 | JFDI | Gener8)"
       - Line 11: Change "(from MaestroView.vue)" to keep as is (MaestroView is the Vue reference file name)

    Use search-and-replace for accuracy. Do NOT modify any other code.
  </action>
  <verify>
    Run: `grep -n "stereos" IP/plugins/06_maestro.py` - should return no results
    Run: `grep -n "orchestr8" IP/plugins/06_maestro.py` - should show brand, CSS classes
  </verify>
  <done>
    No "stereos" references remain in 06_maestro.py (case-insensitive search returns nothing).
    All CSS classes use `.orchestr8-*` prefix.
    Brand displays as "orchestr8" not "stereOS".
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docstrings in woven_maps modules</name>
  <files>IP/woven_maps.py, IP/woven_maps_nb.py</files>
  <action>
    Update docstring references in both Woven Maps files:

    1. **IP/woven_maps.py**:
       - Line 12: Change "Aligned with stereOS Maestro vision" to "Aligned with Maestro vision"
       - Line 110: Change "matches stereOS three-state system" to "matches orchestr8 three-state system"

    2. **IP/woven_maps_nb.py**:
       - Line 20: Change "Aligned with stereOS Maestro vision" to "Aligned with Maestro vision"
       - Line 118: Change "matches stereOS three-state system" to "matches orchestr8 three-state system"

    These are documentation-only changes. No functional code is modified.
  </action>
  <verify>
    Run: `grep -rn "stereos" IP/woven_maps*.py` - should return no results
    Run: `grep -n "orchestr8" IP/woven_maps.py` - should show docstring reference
  </verify>
  <done>
    No "stereos" references remain in woven_maps.py or woven_maps_nb.py.
    Docstrings reference "orchestr8" color system.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify no active code references remain and commit</name>
  <files>None (verification only)</files>
  <action>
    1. Run comprehensive search for remaining "stereOS" references in active code:
       `grep -rn --include="*.py" "stereos" IP/`

    2. Exclude known reference-only locations:
       - Archive/ (historical files)
       - one integration at a time/ (staged integrations, reference files)
       - SOT/, .planning/ (documentation describing the change)

    3. If any active code files still contain "stereos", fix them.

    4. Run the application to verify it still works:
       `marimo run orchestr8.py` (verify no import errors, plugin loads)

    5. Commit changes with message:
       "feat(branding): replace stereOS with orchestr8 branding

       - Update brand display text in 06_maestro.py
       - Rename CSS classes from .stereos-* to .orchestr8-*
       - Update docstrings in woven_maps modules
       - Closes BRAND-01, BRAND-02, BRAND-03"
  </action>
  <verify>
    Run: `grep -rn --include="*.py" "stereos" IP/` - should return no results
    Run: `marimo run orchestr8.py` - should start without errors (Ctrl+C to exit)
  </verify>
  <done>
    All active Python code uses "orchestr8" branding.
    Application starts successfully.
    Changes committed to git.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Visual verification** (manual):
   - Run `marimo run orchestr8.py`
   - Navigate to The Void tab (06_maestro plugin)
   - Confirm top row shows "orchestr8" brand (not "stereOS")
   - Open browser DevTools, search for ".orchestr8-" in Elements/Styles

2. **Code verification** (automated):
   - `grep -rn --include="*.py" "stereos" IP/` returns no results
   - `grep -rn "orchestr8-brand" IP/plugins/06_maestro.py` returns matches

3. **Docstring verification**:
   - `grep -n "orchestr8" IP/woven_maps.py` shows color system reference
</verification>

<success_criteria>
- [ ] User sees "orchestr8" in browser tab/page header (not "stereOS")
- [ ] CSS classes use `.orchestr8-*` prefix (not `.stereos-*`)
- [ ] Docstrings reference "orchestr8" (not "stereOS")
- [ ] Application runs without errors
- [ ] Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/01-branding/01-01-SUMMARY.md`
</output>
